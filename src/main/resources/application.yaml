spring:
  datasource:
    url: jdbc:mysql://localhost:3311/spring
    username: root
    password: StrTit24815032006*
  h2:
    console:
      enabled: false
  jpa:
    hibernate:
      ddl-auto: update
api:
  security:
    token:
      secret: ${JWT_SECRET:pastaMasterChef}