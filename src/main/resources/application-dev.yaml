spring:
  datasource:
    url: jdbc:mysql://localhost:3306/spring
    username: teste
    password: root
  h2:
    console:
      enabled: false
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true