server:
  address: 192.168.88.80
spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:dev
    username: alvaro
    password:
  h2:
    console:
      enabled: false
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true